function [resultado,precision] = funcion_ejemplo(num1, num2)
  precision = 0.00001;      % Se le da valor a lo que se retorna
  disp("Hola, desde la funcion");
  resultado = num1^num2;    % Se le da valor a lo que se retorna
end
